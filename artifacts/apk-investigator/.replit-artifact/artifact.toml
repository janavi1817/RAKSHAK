kind = "web"
previewPath = "/"
title = "APK Fraud Intelligence Investigator"
version = "1.0.0"
id = "artifacts/apk-investigator"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20069

[services.development]
run = "pnpm --filter @workspace/apk-investigator run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apk-investigator", "run", "build" ]
publicDir = "artifacts/apk-investigator/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20069"
BASE_PATH = "/"
